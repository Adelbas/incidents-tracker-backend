server:
  port: 8080

spring:
  datasource:
    url: "jdbc:postgresql://localhost:5434/incidents_api?currentSchema=public"
    username: postgres
    password: postgres
  kafka:
    bootstrap-servers: localhost:9092

kafka:
  consumer:
    group-id: api-gateway

api:
  client:
    incident:
      rest:
        host: localhost
        port: 8081
